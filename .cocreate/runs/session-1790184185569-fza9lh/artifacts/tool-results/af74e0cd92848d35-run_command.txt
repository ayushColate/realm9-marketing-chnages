Started in the background as job "vt-1790186114748-120" in terminal "main".
(started in background as job vt-1790186114748-120 in terminal "main" — you'll get a tool result when it finishes; TERMINAL_READ or poll to check sooner)
It's running in the Cana Agent terminal (separate from the user's). You are NOT blocked — you'll receive a terminal-job-done result automatically when it finishes. Do not poll in a loop; use TERMINAL_READ (or poll once) only if you need intermediate output.